<%= simple_form_for [@restaurant, @review], url: new_review_path, method: :post do |f| %>
  <%= f.input :content %>
    <%= f.input :rating, collection: 0..5 %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
